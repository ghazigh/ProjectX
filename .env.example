# Copy to .env and fill in. Never commit the real .env.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the default model (opus is highest quality;
# claude-sonnet-4-6 is ~40% cheaper for high-volume client work).
CONTENT_ENGINE_MODEL=claude-opus-4-7
